// Reverse path notation
#include <iostream>
#include <string>

int main()
{
    std::string exp;
    std::cout << "Enter the expression:\n";
    std::cin >> exp;
    for(int i = 0; i <= exp.length(); i++){
        //I: (a+t)
        //O: at+
        // 1. Interchange the string
        // 2. Remove the bracket
        
    }
}